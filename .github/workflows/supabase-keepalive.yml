name: Supabase Keepalive

on:
  schedule:
    - cron: '0 12 */3 * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API (orça•me)
        run: |
          curl -s "https://alrpnvctcooernsdddax.supabase.co/rest/v1/" \
            -H "apikey: sb_publishable_kszH2B5zQuSOHUZTxSTNHw_Vit34hNT" \
            -o /dev/null -w "HTTP status: %{http_code}\n"
